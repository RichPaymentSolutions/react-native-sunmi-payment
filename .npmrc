# GitHub Packages scope mapping for this package.
# Teammates only need to export GITHUB_TOKEN from `gh auth token`.
@richpaymentsolutions:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken="$(gh auth token)"
always-auth=true
